# Core
NODE_ENV=development
#
# Database (Postgres) — Prisma reads DATABASE_URL + DIRECT_URL from prisma/schema.prisma
#
# --- Neon (https://console.neon.tech) ---
# 1) Open your project → Branches → select branch → Connection details.
# 2) DATABASE_URL: use the "Pooled" / pooler connection string for serverless (host often contains "-pooler"
#    or "pooler"; required for Vercel / many concurrent connections).
# 3) DIRECT_URL: use the "Direct" (non-pooled) connection for Prisma Migrate (`prisma migrate deploy`).
#    Both must be the same Neon branch/database; use sslmode=require (Neon includes it in the copy-paste URL).
# 4) After changing URLs: `npx prisma migrate deploy` then `npm run db:status` (or `npx prisma migrate status`).
# 5) Optional: `npm run db:indexes:platform-backend` for extra SQL indexes (see scripts/sql/).
# Cold starts: Neon → Project settings → disable autosuspend or upgrade compute if you see DB timeouts.
#
# --- Legacy Supabase pooler note ---
# If migrating from Supabase, replace both URLs entirely; do not mix pooler hosts across providers.
#
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
DIRECT_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
#
# Troubleshooting `P6001` / "the URL must start with the protocol prisma://"
# - This app connects with **postgresql://** (see lib/env/database-url.ts). Do not set DATABASE_URL
#   to a Prisma Accelerate `prisma://` URL unless you intentionally wire Accelerate end-to-end.
# - Vercel may expose POSTGRES_URL / POSTGRES_PRISMA_URL as postgres while DATABASE_URL is still
#   prisma:// — lib/prisma.ts aligns process.env with the resolved postgres URL at runtime.
# - If the error persists after URLs are correct: regenerate the client **without** `--no-engine`
#   (`npx prisma generate`). `--no-engine` builds an edge client that requires `prisma://` URLs.

# Discord integration (OAuth client_id 1482905012542767275 is public / hardcoded in app)
DISCORD_CLIENT_SECRET=
DISCORD_BOT_TOKEN=
# Optional: override OAuth redirect (default production https://www.allfantasy.ai/api/auth/discord/callback)
# DISCORD_REDIRECT_URI=
# DISCORD_BOT_REDIRECT_URI=

# Auth / Sessions
NEXTAUTH_SECRET="replace-with-secure-random"
NEXTAUTH_URL="http://127.0.0.1:3000"
SESSION_SECRET="replace-with-secure-random"
ADMIN_SESSION_SECRET="replace-with-secure-random"
ADMIN_PASSWORD="replace-with-strong-password"
ADMIN_PASSWORD_HASH=""
ADMIN_EMAILS=""

# Comma-separated App user IDs (Prisma `AppUser.id`, must match `session.user.id` — copy from /api/auth/session).
# Full AI + subscription + token bypass: any of the lists below (see `isSubscriptionEntitlementBypassUserId` in lib/dev-admin/access.ts).
# DEV_ADMIN_USER_IDS=""
# TOKEN_NOTIFICATION_BYPASS_USER_IDS=""  (suppresses token nudges AND grants same entitlement/token bypass as dev admin for these ids)
# AI_ENTITLEMENT_BYPASS_USER_IDS=""  (optional explicit QA list; same effect as DEV_ADMIN for entitlements)

# App URLs
APP_URL="http://127.0.0.1:3000"
# For scripts (smoke tests, QA, stress tests): base URL of running app (default 3000)
BASE_URL="http://127.0.0.1:3000"
PUBLIC_SITE_URL="http://127.0.0.1:3000"
NEXT_PUBLIC_SITE_URL="http://127.0.0.1:3000"
NEXT_PUBLIC_APP_URL="http://127.0.0.1:3000"
#
# Canonical host (www vs apex): keep NEXT_PUBLIC_SITE_URL, PUBLIC_SITE_URL, NEXT_PUBLIC_APP_URL, and
# NEXTAUTH_URL on the same origin in production so SEO metadata, emails, OAuth callbacks, and
# middleware host redirects stay aligned (see lib/site-public-origin.ts and middleware
# canonicalProductionHostRedirect).

# --- Google Sign-In (NextAuth) — GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET in lib/auth.ts ---
# If Google shows "Error 400: redirect_uri_mismatch", the OAuth client is missing an authorized redirect,
# or NEXTAUTH_URL does not match the URL you open (localhost vs 127.0.0.1, http vs https, www vs apex).
# Google Cloud Console → APIs & Services → Credentials → your OAuth 2.0 Web client:
#   Authorized JavaScript origins: e.g. http://localhost:3000, http://127.0.0.1:3000, https://www.allfantasy.ai
#   Authorized redirect URIs — add each exact callback NextAuth uses (no trailing slash on the path):
#     {NEXTAUTH_URL}/api/auth/callback/google
#   Register every origin you use, for example:
#     http://localhost:3000/api/auth/callback/google
#     http://127.0.0.1:3000/api/auth/callback/google
#     https://www.allfantasy.ai/api/auth/callback/google
#   Vercel previews: add https://<preview-branch>.vercel.app/api/auth/callback/google per deployment,
#   or test OAuth only on a stable staging URL.
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=

# Devy league creation (optional) — `components/league-creation-wizard/DevyLeagueSetupSection.tsx`
# Omit or leave unset/falsey: only "Standard Devy" shows in Devy mode. Set to "1" to enable extra modes.
NEXT_PUBLIC_DEVY_C2C=""
NEXT_PUBLIC_DEVY_EXPANDED_PIPELINE=""

# Create League wizard (step 1) — optional CDN base for sport preview MP4s. When set, sport tiles resolve to
# `{base}/{sport}.mp4` (lowercase sport: nfl, nba, …). See `lib/league-creation/concept-intro-videos.ts` and
# `getSportSelectionVideoUrl` in `lib/league-media/resolveOptionMedia.ts`. Omit to use bundled `/public` clips.
NEXT_PUBLIC_LEAGUE_CONCEPT_VIDEO_BASE=""

# League chat composer — GIF search (DB + Giphy) and Vercel Blob uploads
GIPHY_API_KEY=
# Dev fallback: dc6zaTOxFJmzC (Giphy public beta; rate-limited) — prefer a real key from https://developers.giphy.com/
BLOB_READ_WRITE_TOKEN=

# OpenAI (canonical: OPENAI_API_KEY)
OPENAI_API_KEY=""
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_MODEL="gpt-4o"
# OpenAI speech fallback (e.g. legacy `/api/chimmy/voice` when ElevenLabs is unavailable)
OPENAI_TTS_MODEL="tts-1"
OPENAI_TTS_VOICE="nova"
# Legacy / integration alias
AI_INTEGRATIONS_OPENAI_API_KEY=""
AI_INTEGRATIONS_OPENAI_BASE_URL="https://api.openai.com/v1"

# ElevenLabs TTS (Chimmy voice — `/api/tts` and `/api/chimmy/voice`)
# Get API key and voice IDs from https://elevenlabs.io
ELEVENLABS_API_KEY=
# Primary Chimmy voice (e.g. Allison). If unset, defaults to Rachel premade below.
ELEVENLABS_VOICE_ID=
# Optional overrides; default Rachel premade: 21m00Tcm4TlvDq8ikWAM
ELEVENLABS_RACHEL_VOICE_ID=
ELEVENLABS_ADAM_VOICE_ID=
ELEVENLABS_USAGE_API_KEY=
ELEVENLABS_MODEL_ID="eleven_turbo_v2"

# Openclaw Dev Assistant (server-side only)
OPENCLAW_WEBUI_URL="https://webui.clawship.ai/allfantasy-dev-assistant/"
OPENCLAW_GATEWAY_URL="wss://webui.clawship.ai/allfantasy-dev-assistant/ws"
OPENCLAW_TOKEN=""
# Openclaw Growth / Marketing Assistant (server-side only)
OPENCLAW_GROWTH_WEBUI_URL="https://webui.clawship.ai/allfantasy-growth-marketing-assistant/"
OPENCLAW_GROWTH_GATEWAY_URL="wss://webui.clawship.ai/allfantasy-growth-marketing-assistant/ws"
OPENCLAW_GROWTH_TOKEN=""
# Optional comma-separated host allowlist for OPENCLAW_WEBUI_URL / OPENCLAW_GATEWAY_URL
OPENCLAW_ALLOWED_HOSTS="webui.clawship.ai"

# DeepSeek (canonical: DEEPSEEK_API_KEY)
DEEPSEEK_API_KEY=""
DEEPSEEK_BASE_URL="https://api.deepseek.com/v1"
DEEPSEEK_MODEL="deepseek-chat"

# DeepL translation fallback (server-side only; optional)
DEEPL_API_KEY=""
DEEPL_API_BASE_URL="https://api-free.deepl.com"

# xAI / Grok (canonical: XAI_API_KEY)
XAI_API_KEY=""
XAI_MODEL="grok-2-latest"
XAI_BASE_URL="https://api.x.ai/v1"
# Legacy
GROK_API_KEY=""
GROK_MODEL="grok-4-0709"
GROK_BASE_URL="https://api.x.ai/v1"
GROK_TIMEOUT_MS="12000"
GROK_ENRICH_ENABLED="false"
GROK_ENRICH_MAX="8"
GROK_ENRICH_CONCURRENCY="3"
GROK_ENRICH_WAIVERS_ENABLED="false"
GROK_ENRICH_WAIVERS_MAX="10"
GROK_ENRICH_WAIVERS_CONCURRENCY="3"

# ClearSports (canonical: CLEARSPORTS_API_KEY + CLEARSPORTS_API_BASE)
CLEARSPORTS_API_KEY=""
CLEARSPORTS_API_BASE=""
# Optional: timeout (ms), retries, rate limit per minute (PROMPT 153)
# CLEARSPORTS_TIMEOUT_MS=15000
# CLEARSPORTS_MAX_RETRIES=2
# CLEARSPORTS_RATE_LIMIT_PER_MINUTE=60
# Optional: feature gates + health probe (PROMPT 153)
# CLEARSPORTS_PROJECTIONS_ENABLED=false
# CLEARSPORTS_NEWS_ALERTS_ENABLED=false
# Legacy
CLEAR_SPORTS_API_KEY=""
CLEAR_SPORTS_API_BASE=""

# AI Orchestration (optional; defaults used if unset)
# AI_ORCHESTRATION_TIMEOUT_MS=25000
# AI_ORCHESTRATION_RETRY_COUNT=1
# AI_PROVIDER_HEALTHCHECK_TIMEOUT_MS=2500
# AI_FALLBACK_MIN_PROVIDERS=1
# AI_QUALITY_GATE_STRICT_REJECT=false
# AI_QUALITY_GATE_MAX_WARNINGS=2

# News / Search / Weather
NEWSAPI_KEY=""
NEWS_API_KEY=""
SERPER_API_KEY=""
OPENWEATHERMAP_API_KEY=""

# Sports data providers
ROLLING_INSIGHTS_API_KEY=""
# Rolling Insights DataFeeds — REST-first + OAuth (GraphQL fallback) (https://datafeeds.rolling-insights.com). Set 1: NFL
ROLLING_INSIGHTS_CLIENT_ID=""       # from accounts.rolling-insights.com
ROLLING_INSIGHTS_CLIENT_SECRET=""   # shown once at creation
# Rolling Insights DataFeeds — Set 2 (NBA, MLB, NHL, Soccer, NCAABB, NCAAFB)
ROLLING_INSIGHTS_CLIENT_ID2=""
# Also used as REST `RSC_token` for /api/v1/player-info and /teams (not CLIENT_ID2)
ROLLING_INSIGHTS_CLIENT_SECRET2=""
# Optional direct RSC token (used by admin probe scripts and explicit REST diagnostics)
# ROLLING_INSIGHTS_RSC_TOKEN=""
ROLLING_INSIGHTS_REST_BASE="https://rest.datafeeds.rolling-insights.com"
ROLLING_INSIGHTS_BASE_URL="https://datafeeds.rolling-insights.com"
RI_NFL_ENABLED="true"
RI_NBA_ENABLED="false"
RI_MLB_ENABLED="false"
RI_NHL_ENABLED="false"
RI_NCAAF_ENABLED="false"
RI_NCAAB_ENABLED="false"
RI_SOCCER_ENABLED="false"
API_SPORTS_KEY=""
THESPORTSDB_API_KEY=""
THEAUDIODB_API_KEY=""
THESPORTSDB_NCAAM_LEAGUE_ID="4607"
THESPORTSDB_NCAAF_LEAGUE_ID=""
THESPORTSDB_SOCCER_LEAGUE_ID=""
ESPN_SOCCER_PATH="soccer/eng.1"
CFBD_KEY=""

# Yahoo OAuth
YAHOO_CLIENT_ID=""
YAHOO_CLIENT_SECRET=""
YAHOO_REDIRECT_URI="http://localhost:3000/api/league/yahoo/callback"

# League auth encryption (required for encrypted external credentials)
LEAGUE_AUTH_ENCRYPTION_KEY="replace-with-32+-char-secret"
# Cron auth secrets: blank/whitespace values are treated as unset at runtime.
LEAGUE_CRON_SECRET=""
AI_ADP_CRON_SECRET=""
AI_ADP_DAILY_CRON_UTC="0 6 * * *"
AI_ADP_LOOKBACK_DAYS="120"
AI_ADP_LOW_SAMPLE_THRESHOLD="5"
AI_ADP_MIN_SAMPLE_SIZE="2"

# 3-level AI learning (deterministic aggregates from `af_learning_events`; not LLM training)
# Rolling window for snapshot recompute (cron + admin POST /api/admin/af-learning).
AF_LEARNING_RECOMPUTE_WINDOW_DAYS="90"
# Emergency: set to "false" to skip attaching `learningLayers` on standard AI payloads.
AF_LEARNING_PAYLOAD_ENABLED=""

# Redis / queues (optional unless queue features enabled). On Vercel use a hosted TCP URL (e.g. Upstash).
# Do not set REDIS_URL to "/" or a non-redis URL — that causes ioredis to treat it as a Unix socket (EROFS on serverless).
# Leave REDIS_URL empty if you are not using Redis; the app falls back to in-memory caches where supported.
REDIS_URL=""
REDIS_HOST=""
REDIS_PORT="6379"

# Email / SMS / Billing
# Resend: required for /forgot-password and other outbound mail. Without RESEND_API_KEY, reset links are not emailed
# (API still returns success to avoid revealing whether an email exists). Verify your sender domain in Resend and set RESEND_FROM.
RESEND_API_KEY=""
RESEND_FROM="AllFantasy.ai <noreply@allfantasy.ai>"
RESEND_FROM_EMAIL="AllFantasy <no-reply@allfantasy.ai>"

# Twilio supports either mode:
# 1) Account SID + Auth Token, or
# 2) Account SID + API Key + API Secret
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_API_KEY=""
TWILIO_API_SECRET=""
TWILIO_PHONE_NUMBER=""
TWILIO_VERIFY_SERVICE_SID=""

STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Monetization — Stripe Price IDs (Dashboard) for subscription + token catalog (`lib/monetization/catalog.ts`)
STRIPE_PRICE_AF_PRO_MONTHLY=""
STRIPE_PRICE_AF_PRO_YEARLY=""
STRIPE_PRICE_AF_COMMISSIONER_MONTHLY=""
STRIPE_PRICE_AF_COMMISSIONER_YEARLY=""
STRIPE_PRICE_AF_WAR_ROOM_MONTHLY=""
STRIPE_PRICE_AF_WAR_ROOM_YEARLY=""
STRIPE_PRICE_AF_SUPREME_MONTHLY=""
STRIPE_PRICE_AF_SUPREME_YEARLY=""
STRIPE_PRICE_AF_TOKENS_5=""
STRIPE_PRICE_AF_TOKENS_10=""
STRIPE_PRICE_AF_TOKENS_25=""
# Stripe Payment Links (buy.stripe.com) — used by checkout client reference id flow
STRIPE_CHECKOUT_LINK_AF_PRO_MONTHLY=""
STRIPE_CHECKOUT_LINK_AF_PRO_YEARLY=""
STRIPE_CHECKOUT_LINK_AF_COMMISSIONER_MONTHLY=""
STRIPE_CHECKOUT_LINK_AF_COMMISSIONER_YEARLY=""
STRIPE_CHECKOUT_LINK_AF_WAR_ROOM_MONTHLY=""
STRIPE_CHECKOUT_LINK_AF_WAR_ROOM_YEARLY=""
STRIPE_CHECKOUT_LINK_AF_SUPREME_MONTHLY=""
STRIPE_CHECKOUT_LINK_AF_SUPREME_YEARLY=""
STRIPE_CHECKOUT_LINK_AF_TOKENS_5=""
STRIPE_CHECKOUT_LINK_AF_TOKENS_10=""
STRIPE_CHECKOUT_LINK_AF_TOKENS_25=""

# Optional commerce / analytics
COINBASE_COMMERCE_API_KEY=""
COINBASE_API_KEY=""
PAYPAL_CLIENT_ID=""
PAYPAL_CLIENT_SECRET=""

META_CONVERSIONS_API_TOKEN=""
META_PIXEL_ID=""
META_TEST_EVENT_CODE=""
NEXT_PUBLIC_META_PIXEL_ID=""
NEXT_PUBLIC_FB_APP_ID=""
NEXT_PUBLIC_GA_MEASUREMENT_ID=""

# Social auth toggles for frontend buttons
NEXT_PUBLIC_ENABLE_GOOGLE_AUTH="false"
NEXT_PUBLIC_ENABLE_APPLE_AUTH="false"

# Dev-only switches
# Set to "true" to register /sw.js on localhost (e.g. PWABuilder against next dev). Production always registers.
NEXT_PUBLIC_ENABLE_PWA_SW="false"
# Set to "1" on Chromium-targeted deployments to serve /manifest.experimental.webmanifest.
# Leave unset (or "0") to use the standards-compliant /manifest.webmanifest.
NEXT_PUBLIC_PWA_EXPERIMENTAL_MANIFEST="0"
# DB-first development mode (page-load guardrails + local cache-first testing)
AF_USE_DB_CACHE_ONLY="false"
AF_DISABLE_LIVE_API_ON_PAGE_LOAD="false"
AF_DISABLE_IMAGE_LOOKUP_ON_PAGE_LOAD="false"
AF_DISABLE_ADP_LIVE_MERGE_ON_PAGE_LOAD="false"
AF_DISABLE_STATS_LIVE_MERGE_ON_PAGE_LOAD="false"
AF_DISABLE_AI_LIVE_CALLS="false"
AF_MOCK_AI_CACHE_FIRST="false"
AF_DRAFT_POOL_CACHE_TTL_SECONDS="300"
DEV_SIM_TOKEN=""
BRACKET_DEV_SECRET=""
NEXT_PUBLIC_BRACKET_DEV_SECRET=""
BRACKET_ADMIN_SECRET=""
# World Cup cron auth prefers BRACKET_CRON_SECRET, then LEAGUE_CRON_SECRET, then CRON_SECRET.
BRACKET_CRON_SECRET=""
EARLY_ACCESS_CONFIRM_SECRET=""
EARLY_ACCESS_SYNC_SECRET=""
EARLY_ACCESS_SYNC_ALLOWED_ORIGINS=""
USE_MOCK_PROVIDER="false"

# Push notifications (optional; web-push VAPID)
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_MAILTO="mailto:noreply@allfantasy.ai"

# Optional hosting envs (typically auto-set in deployment)
VERCEL_URL=""
APP_DOMAINS=""
NEXT_PUBLIC_FANCRED_URL="https://fancred.app"

# Geo-restriction & VPN detection (optional — Vercel geo headers provide state for U.S. visitors)
# Improves VPN/proxy detection accuracy; without keys, geo still works from Vercel headers.
PROXYCHECK_API_KEY=
IPAPI_KEY=

# AutoCoach Status Worker — all optional; core path uses api-chain injuries + Sleeper NFL/NBA.
# BullMQ worker: set START_AUTOCOACH_STATUS_WORKER_WITH_NEXT=1 on a dedicated Node host with Redis (not Vercel serverless).
# Without these keys, optional sources skip gracefully; cron still enqueues jobs when Redis is configured.
XAI_API_KEY=
GROK_API_KEY=
NEWS_API_KEY=
GOOGLE_SEARCH_API_KEY=
GOOGLE_SEARCH_CX=
START_AUTOCOACH_STATUS_WORKER_WITH_NEXT=


