# MLB Rolling Insights — documented mapping (partial)

Batting and pitching live boxes map separately (`live_batting`, `live_pitching`). Schedule entries may include `away_pitcher` / `home_pitcher` player ids.
