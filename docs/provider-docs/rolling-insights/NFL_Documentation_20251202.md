# Rolling Insights — NFL API (owner reference stub)

The canonical **field inventory and AllFantasy mapping** for Rolling Insights NFL live in:

- `docs/provider-docs/rolling-insights/nfl-field-map.md`

This file exists so the repo has a stable path for the owner-uploaded NFL documentation reference (`20251202`). **Do not invent fields here.** When the full vendor Markdown/PDF is checked in, replace this stub with that content verbatim.

**Auth:** `RSC_token` query parameter.  
**Sport path segment:** `NFL`.
