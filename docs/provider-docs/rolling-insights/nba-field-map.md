# NBA Rolling Insights — documented mapping (partial)

See `lib/providers/rollingInsightsFieldMaps.ts` (`NBA` keys). Live `player_box` stats include points, assists, rebounds, shooting splits; schedules use `game_ID`, teams, `broadcast`, etc.
