/**
 * The single canonical read-only explanation for imported leagues. Use SPARINGLY (once per surface,
 * not on every card). Shared so the wording never drifts.
 */
export const IMPORTED_LEAGUE_READONLY_NOTE =
  'AllFantasy analyzes and recommends. Changes to imported leagues are completed on the original platform.'
