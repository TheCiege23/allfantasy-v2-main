'use client'
/** Compatibility export: personality badges are no longer exposed. */
export function ManagerStyleBadge(_props: { leagueId: string; managerId: string; className?: string }) {
  return null
}
